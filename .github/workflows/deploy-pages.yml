name: 部署到 GitHub Pages

on:
  push:
    branches: [ main ]
    paths:
      - 'page/**'
      - 'archive/**'
      - '.github/workflows/deploy-pages.yml'
  
  # 定时执行 - 每天 UTC 2:00 (北京时间 10:00)
  schedule:
    - cron: '0 2 * * *'

  # 允许手动触发
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# 防止并发部署
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # 构建作业
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Copy data to page directory
        run: |
          # 将archive复制到page目录，使其可在GitHub Pages中访问
          if [ -d "archive" ]; then
            cp -r archive page/
            echo "✅ 数据文件已复制到page目录"
            
            # 验证复制结果
            echo "📊 复制统计:"
            echo "  - 国家目录数: $(ls page/archive | wc -l)"
            echo "  - JSON文件总数: $(find page/archive -name '*.json' | wc -l)"
            echo "  - 总文件大小: $(du -sh page/archive | cut -f1)"
            
            # 显示示例文件
            echo -e "\n📁 目录结构示例:"
            ls -la page/archive | head -5
            
            if [ -f "page/archive/China/2025-08-28.json" ]; then
              echo "✅ 测试文件存在: China/2025-08-28.json"
            else
              echo "⚠️ 测试文件不存在: China/2025-08-28.json"
            fi
          else
            echo "❌ 错误: archive 目录不存在"
            exit 1
          fi

      - name: Create data index
        run: |
          # 创建数据索引文件
          cat > page/data-index.js << 'EOF'
          // 自动生成的数据索引
          window.WALLPAPER_DATA_INDEX = {
            generated: new Date().toISOString(),
            countries: [],
            dates: [],
            totalFiles: 0,
            availableData: {}
          };
          
          // 扫描可用数据
          const fs = require('fs');
          const path = require('path');
          
          const dataDir = path.join(__dirname, '..', 'archive');
          const index = window.WALLPAPER_DATA_INDEX;
          
          if (fs.existsSync(dataDir)) {
            const countries = fs.readdirSync(dataDir).filter(item => 
              fs.statSync(path.join(dataDir, item)).isDirectory()
            );
            
            countries.forEach(country => {
              const countryPath = path.join(dataDir, country);
              const files = fs.readdirSync(countryPath).filter(item =>
                item.endsWith('.json') && fs.statSync(path.join(countryPath, item)).isFile()
              );
              
              if (!index.availableData[country]) {
                index.availableData[country] = [];
              }
              
              files.forEach(file => {
                // 从文件名获取日期（去掉.json扩展名）
                const date = file.replace('.json', '');
                index.availableData[country].push(date);
                index.totalFiles++;
              });
              
              if (index.availableData[country].length > 0) {
                index.countries.push(country);
                index.dates = [...new Set([...index.dates, ...index.availableData[country]])];
              }
            });
            
            index.dates.sort().reverse();
          }
          EOF
          
          # 使用Node.js生成实际的索引
          node -e "
          const fs = require('fs');
          const path = require('path');
          
          console.log('🔍 开始生成数据索引...');
          
          const index = {
            generated: new Date().toISOString(),
            countries: [],
            dates: [],
            totalFiles: 0,
            availableData: {}
          };
          
          const dataDir = 'page/archive';
          
          if (fs.existsSync(dataDir)) {
            console.log('📁 扫描数据目录:', dataDir);
            const countries = fs.readdirSync(dataDir).filter(item => 
              fs.statSync(path.join(dataDir, item)).isDirectory()
            );
            
            console.log('🌍 发现国家目录:', countries.length, '个');
            
            countries.forEach(country => {
              const countryPath = path.join(dataDir, country);
              const files = fs.readdirSync(countryPath).filter(item =>
                item.endsWith('.json') && fs.statSync(path.join(countryPath, item)).isFile()
              );
              
              if (!index.availableData[country]) {
                index.availableData[country] = [];
              }
              
              files.forEach(file => {
                const date = file.replace('.json', '');
                index.availableData[country].push(date);
                index.totalFiles++;
                console.log('✅ 发现数据文件:', country + '/' + date);
              });
              
              if (index.availableData[country].length > 0) {
                index.countries.push(country);
                index.dates = [...new Set([...index.dates, ...index.availableData[country]])];
              }
            });
            
            // 按日期倒序排列（最新在前）
            index.dates.sort().reverse();
            
            // 生成索引内容
            const indexContent = [
              '// 自动生成的数据索引文件',
              '// 生成时间: ' + new Date().toISOString(),
              '',
              'window.WALLPAPER_DATA_INDEX = ' + JSON.stringify(index, null, 2) + ';',
              '',
              'console.log(\"📊 数据索引加载完成:\", {',
              '  countries: ' + index.countries.length + ',',
              '  dates: ' + index.dates.length + ',',
              '  totalFiles: ' + index.totalFiles,
              '});'
            ].join('\n');
            
            fs.writeFileSync('page/data-index.js', indexContent);
            
            console.log('✅ 数据索引生成完成:');
            console.log('  - 可用国家:', index.countries.length, '个');
            console.log('  - 可用日期:', index.dates.length, '个');
            console.log('  - JSON文件总数:', index.totalFiles, '个');
            if (index.dates.length > 0) {
              console.log('  - 日期范围:', index.dates[index.dates.length-1], '至', index.dates[0]);
            }
            console.log('  - 索引文件大小:', fs.statSync('page/data-index.js').size, '字节');
            
            // 验证索引文件内容
            const indexFileContent = fs.readFileSync('page/data-index.js', 'utf8');
            if (indexFileContent.includes('WALLPAPER_DATA_INDEX')) {
              console.log('✅ 索引文件内容验证通过');
            } else {
              console.log('❌ 索引文件内容验证失败');
              process.exit(1);
            }
            
            // 输出完整的日期列表用于调试
            console.log('📅 完整日期列表:', JSON.stringify(index.dates));
          } else {
            console.log('❌ 错误: 数据目录不存在:', dataDir);
            process.exit(1);
          }
          "

      - name: Optimize images and create thumbnails
        run: |
          # 这里可以添加图片优化逻辑（可选）
          echo "图片优化步骤（当前跳过）"

      - name: Validate website
        run: |
          # 验证HTML和JSON文件
          echo "验证网站文件..."
          
          # 检查关键文件是否存在
          echo "🔍 验证关键文件..."
          test -f page/index.html || (echo "❌ 错误: index.html 不存在" && exit 1)
          test -f page/assets/css/style.css || (echo "❌ 错误: style.css 不存在" && exit 1)
          test -f page/assets/js/app.js || (echo "❌ 错误: app.js 不存在" && exit 1)
          test -f page/data-index.js || (echo "❌ 错误: data-index.js 不存在" && exit 1)
          test -d page/archive || (echo "❌ 错误: archive 目录不存在" && exit 1)
          
          echo "✅ 关键文件验证通过"
          
          # 显示最终的文件结构
          echo -e "\n📁 最终部署结构:"
          find page -type f | head -20 | sort
          
          # 验证JSON文件格式
          echo "验证 JSON 文件格式..."
          find page/archive -name "*.json" -exec node -e "
            try {
              JSON.parse(require('fs').readFileSync('{}', 'utf8'));
              console.log('✓ {}');
            } catch(e) {
              console.error('✗ {}: ' + e.message);
              process.exit(1);
            }
          " \;
          
          echo "验证通过！"

      - name: Build with Jekyll
        run: |
          # 为了兼容GitHub Pages，添加Jekyll配置
          cat > page/_config.yml << 'EOF'
          # GitHub Pages Jekyll 配置
          title: "必应壁纸展示平台"
          description: "浏览来自全球14个国家/地区的精美必应壁纸"
          baseurl: "/BingWallpaperCollector"
          url: "https://hippiezhou.github.io"
          
          # 排除文件
          exclude:
            - node_modules
            - package*.json
            - "*.md"
          
          # 包含隐藏文件
          include:
            - _headers
            - _redirects
          
          # 插件
          plugins:
            - jekyll-relative-links
          
          # 相对链接
          relative_links:
            enabled: true
            collections: true
          EOF
          
          # 创建.nojekyll文件以禁用Jekyll（如果需要纯静态站点）
          # touch page/.nojekyll

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./page

  # 部署作业
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  # 通知作业（可选）
  notify:
    runs-on: ubuntu-latest
    needs: [build, deploy]
    if: always()
    steps:
      - name: Notify deployment status
        run: |
          if [ "${{ needs.deploy.result }}" == "success" ]; then
            echo "✅ 部署成功!"
            echo "🌐 访问地址: https://hippiezhou.github.io/BingWallpaperCollector/"
          else
            echo "❌ 部署失败!"
          fi
